print('*' * 10)
# This here is an expression; A piece of code that produces a value.
# Here we multiply the String by 10.
# When Python interpreter tries to execute this line,
# It evaluates the expression - which prints 10 asterisks(*).
